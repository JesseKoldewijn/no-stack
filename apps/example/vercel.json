{
  "installCommand": "cd ../.. && bun install",
  "buildCommand": "cd ../.. && bun run --cwd apps/example build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ],
  "functions": {
    "api/index.js": {
      "includeFiles": "dist/**"
    }
  }
}
